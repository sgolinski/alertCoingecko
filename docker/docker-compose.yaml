version: "3.7"

networks:
  alertcoingeckonet_2:
    external: true

services:
  php-cli:
    build:
      dockerfile: docker/Dockerfile
      context: ./..
    volumes:
      - ../:/mnt/app
    restart: always
    networks:
      - alertcoingeckonet_2